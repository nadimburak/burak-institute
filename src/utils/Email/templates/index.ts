import {verificationTemplate} from './userVerification'
import {welcomeTemplate} from './welcomeTemplate'

export const template = {
    welcome: welcomeTemplate,
    verification: verificationTemplate
}